const router = require('express').Router()

module.exports = () =>{
    router.post("/create", )
    router.get("/", )
    router.get("/:id", )
    router.put("/update", )
    router.delete("/remove", )
    return router
}